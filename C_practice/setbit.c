#include<stdio.h>
int main()
{
int n, i, count = 0;
printf("Enter a non negative integer: ");
scanf("%d", &n);
while(n % 2 == 0)
{
printf("1s bit:" );
}
if(n / 2)
{
if(n == 0)
printf("Total bits: ");
}
return 0;
}


